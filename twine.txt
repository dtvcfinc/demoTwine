[[Uncategorized]]
	[app_name]
		jp = Demo twine.
		fr = Demo twine.
		es = `Demo twine. `
	[this_is_a_new_string]
		jp = This is a new string.
		fr = This is a new string.
		es = Esto es una nueva cadena.
	[demo]
		jp = This is a demo
		fr = This is a demo
		es = Esto es una demo.
	[new_string]
		jp = Demo twine.
		fr = Demo twine.
		es = Demo twine.
	[new_string_b]
		jp = Demo twine.
		fr = Demo twine.
		es = Demo twine.
	[yes]
		jp = Yes
		fr = Oui
		es = Sí
	[no]
		jp = No
		fr = Non
		es = No
	[path_not_found_error]
		jp = The file '%@' could not be found.
		comment = An error describing when a path on the filesystem could not be found.
		fr = The file '%@' could not be found.
		es = No se ha encontrado el archivo '%@'.
	[network_unavailable_error]
		jp = The network is currently unavailable.
		comment = An error describing when the device can not connect to the internet.
		fr = The network is currently unavailable.
		es = The network is currently unavailable.
	[dismiss_error]
		jp = Dismiss
		fr = Oui
		es = Yes
	[list_item_separator]
		jp = `, `
		comment = A string that should be placed between multiple items in a list. For example: Red, Green, Blue
		fr = `, `
		es = `, `
	[grave_accent_quoted_string]
		jp = ``%@``
		comment = This string will evaluate to `%@`.
		fr = ``%@``
		es = ``%@``
